name: "First Workflow"

on: push

jobs: 
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: Print Hello 
              run: echo "Hello Rahul! Github Actions is working"